### Fixed (2026-09-25 — #1500, #1501, #1502: three integration-harness reds from the s27 cluster-B cloud run)
- **#1500: the harness's partition-chaos firewalls blocked every CTM replacement while a partition was up.** `cloud_partition_node` labelled them `aether-cluster=<cluster>`. The Hetzner provider counts any firewall with that label as an ingress firewall its per-source selector missed, and refuses to provision. So a minority that CTM evicted during 12-network S06 could not be replaced until the heal deleted the firewalls. The firewalls now carry `aether-chaos-cluster=<cluster>` and never `aether-cluster`. A reused firewall from an earlier run is relabelled, and its legacy `aether-cluster` label is removed. [verified: `aether/tests/integration/test/test-restore-gate.sh` F1, F2 — putting `aether-cluster` back reddens F1 and F2, and dropping the legacy-label removal reddens F2]
- `tools/cloud-reaper.sh` now selects `aether-chaos-cluster` in every mode, so leaked chaos firewalls are still found:
  - `--cluster X` and `--cluster X --strict-cluster` use the exact `aether-chaos-cluster=X`;
  - the catch-all mode uses the bare key.
  A chaos firewall is scoped and protected like the cluster it names, so `test-pg`'s is never deleted. The dry-run listing includes chaos firewalls. [verified: same file, K1–K5 against a stubbed Hetzner API — removing the selectors reddens all five, and removing the owner-column fallback reddens K5, where the protected firewall gets deleted]
- On cloud, S06 measures CTM **replacement**, not rejoin, when the minority is evicted, because CTM deletes the evicted VMs. The comment on its docker-calibrated 30s budget and its failure text now say so. The budget is unchanged, because there is no clean cloud measurement of replacement time yet. [unverified: whether 30s × `TIMEOUT_SCALE` is enough for replacement on cloud once #1500 is fixed; it needs a cloud run] The provider-side guard, which counts any `aether-cluster`-labelled firewall, is tracked separately as #1503.
- **#1501: 02w reported a transient `FoldInProgress` refusal as "unreachable (no node answered)".** `read_amount` took the first live 2xx answer as final. `read_amount` now retries a `failureType` on an explicit allow-list within `TRANSIENT_READ_DEADLINE_S` (default 60s). The allow-list is `ENTITY_TRANSIENT_FAILURE_TYPES` and starts with `FoldInProgress`. A refusal that is still transient at the deadline is a distinct rc 5, reported with its true last body. A body that is neither found, absent nor transient, and a genuine no-answer, are reported as what they are. The same helper serves both the pre-kill and the post-kill readbacks. [verified: `aether/tests/integration/test/test-cloud-helpers.sh` W1–W8, replaying the body captured in the run log. Making the refusal final again, or emptying the allow-list, reddens W1, W2, W4, W6, W7 and W8. Retrying every failure type reddens W3. W4 and W8 keep ABSENT-after-refusal a loss]
- **#1502: 02y scored ACKED-event survival from one best-effort read** and reported "40 missing" when the next read found 81 events. The app read route is `NEAREST` and returns only `events`. The owner's `ownerHeadOffset` comes from the replicas view. Survival and pre-kill readability are now scored on a settled read, which re-reads every partition until one of two things happens:
  - it is **complete**: every acked marker is present;
  - it is **stable**: two consecutive rounds agree on each partition's count and last offset, and no partition is behind its owner's head.
  The limit is `SETTLED_READ_DEADLINE_S` (default 90s). A stable read that still lacks an acked marker fails as loss. A partition no endpoint could read fails as UNMEASURABLE, not as an empty partition scored as loss. Duplicate events for an acked marker are reported as a `DUPLICATES` WARN with their count, instead of being hidden by distinct-marker scoring. [verified: same file, Y1–Y6. Reverting to a single read reddens Y1 (the s27 shape), Y2, Y4 and Y5. Removing the head check reddens Y4. Removing the duplicate WARN reddens Y3. Counting a missing marker as present reddens Y2. Scoring an unreadable partition reddens Y5. A single pre-kill read reddens Y6] [unverified: whether 90s covers a cloud post-kill leaderless window. In s27 it lasted about 2s]
