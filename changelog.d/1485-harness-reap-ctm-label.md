### Fixed (2026-09-24 — #1485: the reap commands printed by `--keep-on-failure` missed CTM replacement VMs)
- **The printed reap commands could not remove replacement VMs.** They named the harness cluster (`test-b`), while CTM labels replacements with the TOML's `[cluster] name` (`cloud-test-b` on the container runtime), because the CLI's `--cluster` override never reaches the persisted cluster config. Both VMs left after the 2026-09-24 run were such replacements. The preserve path now also prints `--cluster <TOML name> --strict-cluster` for each TOML name that differs from the harness names, only for clusters that were in play. It no longer suggests the account-wide bare reaper. With explicit per-arm cluster names the TOML name is shared by every arm, so it prints a warning and a dry-run command instead of a destructive one. The TOML is parsed the way the TOML parser reads it (either quote style, indentation, CR, trailing comments), and any result that is not a valid label name prints nothing rather than a wrong name. [verified: `aether/tests/integration/test/test-restore-gate.sh` P6-P11, T1-T7 — each of eight mutations (dropped dedupe, B-only loop, ungated A, no section gate, prefix header match, no label-grammar check, no `--strict-cluster`, no explicit-name branch) reddens its own case] [mechanism: the raw TOML is POSTed at formation (`BootstrapPhaseFormation`) → `ClusterConfigValue.clusterName` → CTM `ProvisionContext` → `HetznerComputeProvider.buildLabels`] [unverified: not yet exercised on a live failed cloud run]
