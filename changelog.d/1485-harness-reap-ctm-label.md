### Fixed (2026-09-24 — #1485: the reap commands printed by `--keep-on-failure` missed CTM replacement VMs)
- **The printed reap commands could not remove replacement VMs.** They named the harness cluster (`test-b`), while CTM labels replacements with the TOML's `[cluster] name` (`cloud-test-b` on the container runtime). Both VMs left after the 2026-09-24 run were such replacements. The preserve path now also prints a scoped reap for the TOML cluster name when it differs, and no longer suggests the account-wide bare reaper. [verified: `aether/tests/integration/test/test-restore-gate.sh` P6-P7 — removing the new line reddens P6] [mechanism: `cloud-reaper.sh` listed the 2026-09-24 survivors under cluster `cloud-test-b`]
