### Fixed (2026-09-14 — #498: SWIM false-removal of live survivors during the post-kill PASSIVE window)
- **The defect no longer reproduces at the rc4 tip, and nothing pinned that.** #498's evidence (2026-07-19) was a graceful single-node departure from a 5-node cluster invalidating the survivors' leader, the transient PASSIVE window evicting quiet survivor↔survivor links, SWIM holding a live survivor DEAD-stuck (`swimDeadStuck=[sof-1]`), the dial layer withholding the re-dial for up to the 60s higher-id grace, and — with auto-heal off — the false removals cascading into a quorum-loss self-fence. At `2005ea7d2` a survivor's link is refreshed by the transport's own CONTROL-lane keepalive independent of any leader, a previously-connected evicted member bypasses the 60s grace and is force-dialed on the next reconcile tick, and the FSM desired-set reconciler carries no SWIM dial gate — the eviction, stuck-dead and withheld-dial steps of the chain each have a mechanism closing them. [mechanism: `QuicClusterNetwork.sendKeepalivesUnsafe` / `LIVENESS_TTL_PING_INTERVAL_FACTOR`; `QuicClusterNetwork.previouslyConnectedNowEvicted` in `higherIdGracePeriodElapsed`; `QuicClusterNetwork.reconcileAgainstDesired`] The trigger itself — a survivor's leader-election FSM entering QuorumLost on a peer's graceful departure — was not observed on any survivor in four runs (only the departing node logs it); which change removed it is not traced here. `[unverified: the commit that stopped survivor-side leader invalidation on a graceful departure — history is squashed at `935f42fb7`]`
- Added `SurvivorLivenessAfterGracefulKillTest` (Heavy): 5-node in-JVM cluster with DEFAULT SWIM/hello/split timeouts, auto-heal disabled on every CTM (the ticket's "worse, not better" configuration), graceful `killNode` of a non-leader, then one sample per second on every survivor for 90s — past the 60s force-dial grace — of its counted core set, its FSM projection of each other survivor, and its transport link set. It asserts the guarantee the ticket names: the victim leaves counted membership, no survivor ever counts fewer than the four live nodes, no survivor is projected as anything but `Member`, no survivor↔survivor link is absent for longer than one liveness TTL, and every survivor is still running. [verified: 4/4 runs green at `2005ea7d2` — victim out of counted membership on every survivor in 6.9–8.3s, `minCounted=4`, 0 non-Member samples, 0 link-gap samples, zero `swimDeadStuck` lines, no survivor entered PASSIVE; instrument control: black-holing a survivor at sample 10 reddened all three channels (`minCounted=1`, 319 non-Member samples, a 73-sample link gap, and the black-holed node self-fenced out of `allNodes()`)]
- The pin is tagged `Heavy`, which the PR gate excludes: `ci.yml:82` runs `forge-tests` with `-Dfailsafe.excludedGroups=Heavy`, and `heavy-forge.yml` runs the tagged classes only nightly, on dispatch, or on a PR labelled `run-heavy` — so this class does not run on every push. `[unverified: not in the default CI gate]` The scenario is exactly the ticket's (one non-leader leaves five); its neighbours are not covered by it: `[unverified: PASSIVE on survivors when the LEADER leaves]`, `[unverified: a departure that takes the cluster to exactly the majority threshold]`, `[unverified: engine Paused/Syncing transitions on a survivor during a departure]`, and the timing halves (departure ≤75s, link gap ≤40 samples) were measured at 1-min load 9–128, `[unverified: on a box under load <40]`.
- Not verified here: the ticket's own acceptance instrument, `StreamOwnerFailoverTest` phase 9 (RF-restoration after owner kill), cannot run at the tip — it fails in phase 1 on #1066 (`published no stream bindings`, fix in PR #1071). Flipping that phase from sensor to HARD assertion, and retiring the "#498 blocks RF-rebuild" wording in `AbstractStreamOwnerFailover` / `StreamOwnerFailoverTest`, waits for a green run of that class after #1066 lands. `[unverified: stream-layer RF-restoration convergence at the tip]`
