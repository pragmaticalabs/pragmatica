### Fixed (2026-09-24 — #1483: a cluster-B-only cloud integration run leaked CTM replacements and reported "nothing to reap" over live VMs)
- **A run selecting only cluster-B suites never marked its cloud resources provisioned.** The marker was set only after cluster A's bootstrap. So end-of-run teardown skipped the bare orphan sweep, the only reap that matches CTM replacements labelled `cloud-<cluster>` on the container runtime, and `--keep-on-failure` printed "nothing to reap" while 5 VMs were running (2026-09-24). Cluster B's bring-up now sets the marker after its bootstrap returns. When no bootstrap completed, the preserve path now prints a dry-run listing instead of claiming nothing exists, because a failed bootstrap under `--keep-on-failure` keeps its VMs. [verified: `aether/tests/integration/test/test-restore-gate.sh` P1-P5 — removing the new assignment reddens P1, restoring the old message reddens P2; a failed bootstrap and `--skip-deploy` both leave the marker unset]
