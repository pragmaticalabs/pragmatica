### Fixed (2026-09-14 — #850: stale-entry sweeps used a core-only liveness set, so a live worker's placement rows were removed on every reconcile)
- **Confirmed, not just hypothesised: the three reconcile-time stale-entry sweeps diffed KV rows against `activeNodes()`, which is `MembershipFsm.coreCountedMembers()` — worker-free by construction — so every `NodeArtifactKey`, `NodeRoutesKey` and `SliceNodeKey` row a LIVE worker owned classified as stale on every `reconcile()` tick and on every leader activation.** The worker's `NodeArtifactKey` removal is answered by its own NDM with `forceCleanupSlice` (the slice is unloaded while serving), and the CDM's `sliceStates` view lost the placement, so the next tick re-placed it. [mechanism: `ClusterDeploymentState.Active.activeNodes()` → `ctx.coreCountedMembersSupplier()`; `StaleEntryCleaner.cleanupStaleSliceEntries` / `cleanupStaleNodeArtifactEntries` / `cleanupStaleNodeRoutes`; `NodeDeploymentState.Active.handleNodeArtifactRemove` → `handleSliceValueRemove` → `forceCleanupSlice`] [verified: `ClusterDeploymentStateWorkerRemovalTest.LiveWorkerFootprint` — four cases red at `2005ea7d2` (each sweep alone, and `reconcile()`), green after]
- The sweeps now diff against the nodes that own placements: counted core members plus the workers registered in `workerNodes`. A worker enters that set when its WORKER `ActivationDirectiveKey` commits and leaves it only through `handleNodeRemoval` — the worker-leave channel (#731) or the dead-restored-worker sweep — which is the same path that clears its rows directly, so a registered worker's rows are never stale and a departed worker's still are. [mechanism: `StaleEntryCleaner.placementNodes()`] [verified: `LiveWorkerFootprint.staleSweeps_departedWorker_stillRemoveItsRows` — a sweep that keeps every row reddens exactly this control; reverting the hunk reddens exactly the four live-worker cases]
- Not changed: `activeNodes()` itself stays core-scoped for role assignment, `allocatableNodes()` and the CORE_ONLY placement pool; only the stale-entry classification widened. Whether a real deployment routes slices to workers often enough for the unload/re-place churn to have been observed is not established here. `[unverified: end-to-end worker placement churn on a multi-node run]`
