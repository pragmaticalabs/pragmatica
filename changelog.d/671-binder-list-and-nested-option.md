### Fixed (2026-09-04 — #671: notification TOML section names and SMTP defaults were documented wrong)

- The config binder already bound both shapes named in #671 (`List<String>`, recursive `Option<Record>`) on this base — no binder change was needed [verified: integrations/config/config-service/src/test/java/org/pragmatica/config/ProviderBasedConfigServiceTest.java — ListFieldBinding / OptionalNestedRecordBinding]
- Docs named TOML sections the binder never derives (`smtp`/`http`/`retry` instead of `smtp_config`/`http_config`/`retry_config`, matching `NotificationConfig`'s actual field names) — corrected in `resource-reference.md` and `notification-resource-spec.md` [verified: aether/resource/notification/src/test/java/org/pragmatica/aether/resource/notification/NotificationConfigBindingTest.java — config_smtpBackend_bindsNestedSmtpAndRetryFromDocumentedToml / config_httpBackend_bindsNestedHttpConfigFromDocumentedToml]
- Docs claimed working defaults for SMTP `port`/`tls_mode`/`connect_timeout`/`command_timeout`; these are required today because `SmtpConfig` has no binder-readable `DEFAULT` instance, unlike `RetryConfig` [verified: aether/resource/notification/src/test/java/org/pragmatica/aether/resource/notification/NotificationConfigBindingTest.java#config_smtpBackend_omittedTimeoutFields_failsWithMisattributedSectionError]
