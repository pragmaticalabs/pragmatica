### Fixed (2026-09-08 — #928, #931: the feature catalog's Statistics table is generated from its own rows, and no longer disagrees with them)

- **The catalog's `## Statistics` table is now generated by `scripts/catalog-stats.sh` and checked in CI.** It claimed 207 features while the body carried 227. Against the body as it stood *before* #931 — 171 Complete, 23 Partial, 23 Battle-tested, 9 Planned, 1 Critical — the claimed breakdown was wrong in three of five buckets: Complete understated by 11, Partial by 11, Battle-tested overstated by 2. *After* #931 moves row 58 out of `Critical`, the published distribution is 171 Complete, 24 Partial, 23 Battle-tested, 9 Planned = 227, against which the old table's Partial delta reads 12. `[verified: mutation — changing row 5's status expression `Complete` to `Battle-tested` turns `--check` red (exit 1) with a diff; changing the generated `| Complete | 171 |` count expression to `170` also turns it red; the unmutated file is green either side of both]`
- **Every number and worked example in the generated prose is interpolated from the parsed rows, not written as a literal.** A literal inside a block stamped "generated" is #928's own defect one level down, and the check is structurally unable to see one: `generate()` emits both sides of its own comparison, so a hand-written constant agrees with itself no matter what the rows say. `[verified: mutation — inserting one `Planned` row (applied-count checked: 227→228 feature rows) and regenerating, the script at `ac655525e` emitted `"227 rows asserting a capability"` three lines above `| Total | 228 |` and `--check` exited **0**; the same input through the current script emits 228 in both places]`
- **The cited overclaim example is pinned to the row it describes.** The prose names row 39 (`Alert management | Complete`, against `AlertForwarder` never being constructed in production, #926). The row's id, name and status are read from the file, and generation fails rather than publishing an illustration the rows no longer support. `[verified: mutation — changing row 39's status expression to `Partial` (applied-count checked: 1→0 matching lines) fails with "the cited overclaim example, row 39, now reads \"Partial\" not \"Complete\""; deleting row 39 fails with "no longer exists"; both exit 1]`
- **A green run states the size of the set it examined.** `--check` and `--write` both report `227 feature rows across 4 statuses`, so the CI log is self-evidencing rather than a bare exit status — the rule #740 established for `jbct:check`, which was made to say what it examined so a green result could be read as evidence. `[verified: `--check` on the shipped file prints `catalog-stats: ok -- 227 feature rows across 4 statuses match the generated block`; the count comes from the same run that produced the block, and a zero count is refused rather than reported green]`
- **The generated block is idempotent.** Regeneration over an unchanged file reproduces the file byte-for-byte. `[verified: sha256 of `feature-catalog.md` identical across the initial write and two further `--write` runs — fd13ef78…ff60d three times]`
- **A row whose status the legend does not define is a hard failure, never a silent bucket.** The document's own status legend is the authority for the permitted set, so the summary cannot quietly drop a row it fails to classify — which is how a miscategorised row could sit in the denominator and in no category at once (#931). `[verified: mutation — setting row 5's status expression to `Critical`, which the legend no longer defines, fails with "row 5 (line 34) has status \"Critical\", which the status legend does not define"]`
- **The check cannot pass vacuously.** If the legend parses to fewer than two entries, or the counting rule matches no rows, the script fails rather than reporting a green count over nothing. `[verified: mutation — deleting all four legend bullets fails with "parsed 0 status legend entries; the legend is the allowed-status authority and cannot be empty" rather than emitting a table of zeroes]`
- **Adding or removing a feature row moves the published total.** `[verified: mutation — inserting one `Planned` row turns `--check` red with `Planned` 9→10 and `Total` 227→228]`
- **Row 58 (Web dashboard) is `Partial`, not `Critical`, and `Critical` is retired as a status (#931).** `Critical` named an effort/priority axis rather than a completion level, so the row sat in the total while belonging to no status category. Its own note records the Forge dashboard as complete and enumerates the node-management dashboard's missing pieces, which is exactly the legend's definition of `Partial` — "Core implemented, gaps noted". The priority signal it carried is preserved in the callout below the table, relabelled so it cannot be read as a completion level. `[mechanism: judgement against the legend's own definitions; no row now uses `Critical`, and the generated check would reject one that did]`
- **The published counts are labelled claimed, not verified, in the document itself.** Every figure counts what a row asserts about itself; none is checked against the code. The counting rule is stated next to the table so a reader can reproduce the number instead of trusting it. `[mechanism: owner ruling 2026-09-07, `know:` commit 4607e86d0]`
- **The stale "173 features" belief is retired from the four website-correction notes that carried it.** One of them recorded the site's "173 documented features" as verified — "Feature catalog shows 173. This matches" — an exoneration that had since become false. `[verified: `grep -rn '173' aether/docs/.internal/website-corrections/` returns 5 hits — the quoted website claim, three dated retirement notes naming it, and the `#166-173` issue range — where before the change the same command returned 7, of which 6 were live count-uses and none was a retirement note]`
