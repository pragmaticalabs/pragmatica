### Fixed (2026-09-04 — #759: deploy status honesty for redeploys)

- `GET /blueprints/{id}/status` no longer reports a previous attempt's terminal `FAILED`/`ROLLED_BACK` outcome once a redeploy is in flight and healthy; a fresh publish or delete clears the stale outcome record in the same consensus batch as the blueprint write (#818), so the status route's outcome-first check falls through to live `DeploymentMap` aggregation and reports the redeploy's real progress instead. No operator action needed — the route recomputes on every poll. [mechanism: `SliceRoutes.routeBlueprintStatusByOutcome` reads `BlueprintService.outcome(id)` first, falling through to `handleGetBlueprintStatusFromStore` only when it is empty or non-terminal; `BlueprintService.buildAllCommands` bundles a `KVCommand.Remove` of the outcome key into the republish/delete batch]
- [verified: `aether/node/src/test/java/org/pragmatica/aether/api/routes/BlueprintStatusAggregationTest.java#statusRoute_redeployAfterPriorFailure_outcomeCleared_reportsInProgressNotFailed` (route-level redeploy regression); `aether/aether-deployment/src/test/java/org/pragmatica/aether/deployment/cluster/BlueprintPublishOwnershipTest.java#OutcomeClearedAtPublish` (KV-level atomic outcome clearing)]
