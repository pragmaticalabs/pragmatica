### Fixed (2026-09-24 — #1486: on cloud, the stream- and entity-crash suites sent no traffic, and 02w tested an entity slice that was not deployed)
- **`node_app_endpoints` returned nothing on cloud, so 02y and 02w never sent a request.** 0 of 40 publishes and 0 of 40 entity creates were attempted in the 2026-09-24 run, and every durability assertion after them failed against nothing. The cloud branch now resolves one `http://<public-ip>:<app port>` per live core member, CTM replacements included, and skips unresolvable members rather than reading the lookup's stdout diagnostic as an address. 02y now fails loudly when no endpoint resolves, a publish re-pick skips the endpoint that just failed (a killed member stays listed until its departure commits), and a kill with zero publishes ACKED in the window now fails as "crash window not exercised" instead of passing. [verified: `aether/tests/integration/test/test-cloud-helpers.sh` — restoring the old cloud `return 1` reddens it; dropping the re-pick exclusion reddens its case] [unverified: a cloud run with real traffic; the zero-in-window-ack check is not pinned]
- **02w deployed the slice artifact instead of its blueprint and discarded the refusal,** so after a full-drain recovery wiped the initial deployment the suite ran without the entity slice. It now pushes and deploys `test-entity:1.0.0` and logs any failure. [mechanism: the slice jar has no `META-INF/blueprint.toml`; leader rebuilds during 02w logged only the echo and stream-repl blueprints]
- **`retarget` could set `APP_ENDPOINT` to a `[FAIL]` message** when a slice owner's address did not resolve. [mechanism: `|| true` kept `cloud_public_ip`'s stdout diagnostic; now `|| owner_ip=""` plus a bare-host check] [unverified: not pinned by a test]
- **Removed 12-network "Gossip encryption via transport".** It treated `quic_handshake_failures_total` as TLS failures, but that counter is incremented by every failed dial, so the test measured churn. The TLS positive signal remains in "Gossip encryption via config"; the suite charter now marks contract C3 as not checkable with the exposed metrics. [mechanism: `QuicClusterNetwork.onConnectFailed` → `quicMetrics.onHandshakeFailure()` for any dial failure]
