writer        = console
writer.format = {level|min-size=5} {date: HH:mm:ss.SSS} {class-name|min-size=40}::{method|min-size=20}:{line}: {message}
writer.level  = debug

# Reduce noise
level@org.apache.fury = warn
level@io.netty = warn
