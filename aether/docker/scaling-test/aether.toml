# Aether Node Configuration for Docker Scaling Test
# core_max comes from CORE_MAX env var — allows same config for core and worker nodes

[cluster]
# Durable control state — required since #1390 (#1519). Each compose service mounts its own named
# volume at /data, so the journal survives the container being recreated.
consensus_path = "/data/aether-control"

[slice]
repositories = ["builtin", "local"]

[app-http]
enabled = true
port = 8070

[database]
type = "POSTGRESQL"
host = "postgres"
port = 5432
database = "forge"
username = "forge"
password = "forge"
async_url = "postgresql://forge:forge@postgres:5432/forge"

[database.pool_config]
min_connections = 5
max_connections = 20
connection_timeout = "5s"

[tls]
# QUIC transport auto-generates self-signed certs when no explicit certs are provided.
# cluster_secret seeds deterministic CA generation so all nodes share a common trust root.
auto_generate = true
cluster_secret = "scaling-test-secret"

[messaging.click-events]
topic_name = "click-events"
