# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=chaos
cluster=destructive
destructive=true
requires=CAP_CHAOS
blueprint=test-echo
estimated_duration=5m
description="ISOLATED kill-leader repro (Issue 7 matrix)"
