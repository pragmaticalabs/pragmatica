# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=edge-cases
cluster=destructive
destructive=true
requires=
blueprint=test-echo
estimated_duration=3m
description=Concurrent deploys, disruption budget
