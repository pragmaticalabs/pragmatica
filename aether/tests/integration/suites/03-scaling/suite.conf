# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=scaling
cluster=destructive
destructive=true
requires=CAP_SCALING
blueprint=test-echo
estimated_duration=8m
description=Scale up, scale down, quorum safety
