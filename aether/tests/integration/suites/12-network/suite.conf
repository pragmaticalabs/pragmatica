# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=network
cluster=destructive
destructive=true
requires=CAP_NETWORK_PARTITION
blueprint=test-echo
estimated_duration=4m
description=QUIC connectivity, SWIM, gossip
