# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=streaming
cluster=non-destructive
destructive=false
requires=
blueprint=test-persistence
estimated_duration=3m
description=Publish, consume, replication
