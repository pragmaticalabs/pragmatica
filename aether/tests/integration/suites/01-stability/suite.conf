# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=soak
cluster=non-destructive
destructive=false
requires=
blueprint=test-echo
estimated_duration=4h
description="Long-running stability and soak tests"
