# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=security
cluster=destructive
destructive=true
requires=
blueprint=test-echo
estimated_duration=3m
description=Cert rotation, principal injection, route security
