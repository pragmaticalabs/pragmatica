# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=artifacts
cluster=non-destructive
destructive=false
requires=
blueprint=test-full
estimated_duration=2m
description="Push, resolve, replication"
