# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=chaos
cluster=destructive
destructive=true
requires=CAP_CHAOS
blueprint=test-echo
estimated_duration=6m
description="ISOLATED stream crash-durability (#508) — needs a low-churn cluster, see #593"
