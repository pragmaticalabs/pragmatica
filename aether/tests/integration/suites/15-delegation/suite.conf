# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=delegation
cluster=non-destructive
destructive=false
requires=
blueprint=test-echo
estimated_duration=1m
description="Leader-pinned control plane: single stable leader, CDM + deployment surfaces functional on leader (rescoped from removed distributed task-group delegation)"
