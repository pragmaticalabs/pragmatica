# suite.conf -- parsed by run-tests.sh (source'd as shell vars)
tags=delegation
cluster=destructive
destructive=true
requires=
blueprint=test-full
estimated_duration=2m
description="Task assignments, operator reassignment (kills SCALING task's host node)"
