[cluster]
nodes = 7
management_port = 5150
dashboard_port = 8888
app_http_port = 8070

[observability]
depth_threshold = -1

# H2 embedded database (disabled — using external PostgreSQL)
# To revert to H2: set enabled = true and uncomment init_script
[database]
enabled = false
#port = 9092
#name = "forge"
#persistent = true
#init_script = "./schema/pricing-engine.sql"
