prefix = "/api/v1/urls"

[security]
default = "authenticated"
override_policy = "strengthen_only"

[routes]
shorten = "POST /"
resolve = ["GET /{shortCode}", "public"]

[errors]
default = 500
HTTP_404 = ["*NotFound*"]
HTTP_400 = ["*Invalid*"]
