# Build stamp for the slice-processor. Values are injected by Maven resource
# filtering at build time (see slice-processor/pom.xml <resources> filtering).
# Read at processing time by BuildInfo to make a stale locally-installed
# processor jar diagnosable instead of silently reintroducing fixed codegen bugs (#403).
version=${project.version}
buildTimestamp=${maven.build.timestamp}
