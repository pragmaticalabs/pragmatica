# Tinylog configuration for tests
# Minimal logging to keep test output clean

level = warn
writer = console
writer.format = {level}: {message}

# Reduce noise
level@org.apache.fury = warn
level@io.netty = warn
