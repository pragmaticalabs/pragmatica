# Tinylog configuration for E2E tests

writer        = console
writer.format = {date: HH:mm:ss} [{thread}] {level}: {message}

# Default level
level = info

# Suppress verbose wire-level logging from Docker Java client
level@com.github.dockerjava = warn
level@org.apache.hc = warn
level@org.testcontainers = info
